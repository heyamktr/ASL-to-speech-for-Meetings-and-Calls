# CI Workflows

Placeholder. CI will be added once the first PRs land. Planned workflows:

- `frontend.yml` — ESLint + Prettier check + esbuild build
- `backend.yml` — ruff + black + pytest + Docker build
- `model-training.yml` — ruff + black on Python sources

Drop a workflow file (e.g., `frontend.yml`) here when ready.
