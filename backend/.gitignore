# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual env
.venv/
venv/

# Env
.env

# Model weights (large binaries — .pt checkpoints stay local, .onnx is committed for deployment)
models/*.pt
